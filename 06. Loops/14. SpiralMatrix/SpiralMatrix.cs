/* * Write a program that reads a positive integer number N (N < 20) from console and outputs in the
   console the numbers 1 ... N numbers arranged as a spiral. Example for N = 4 */

using System;

class SpiralMatrix
{
    static void Main()
    {
        int inputN = int.Parse(Console.ReadLine());
        int[,] myArr = new int[inputN, inputN];

        // TO DO !!!
    }
}

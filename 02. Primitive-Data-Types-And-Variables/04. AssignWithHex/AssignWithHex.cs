/* Declare an integer variable and assign it with the value 254 in hexadecimal format.
   Use Windows Calculator to find its hexadecimal representation. */

using System;

class AssignWithHex
{
    static void Main()
    {
        int hexadecimalFormat = 0xFE;
        Console.WriteLine("FE is hexadecimal representation of {0}", hexadecimalFormat);
    }
}
